{
  "name": "journal-root",
  "private": true,
  "scripts": {
    "test:server": "cd server && npm test",
    "test:infra": "tsx scripts/validate-deployment.ts",
    "test:all": "npm run test:infra && npm run test:server",
    "lint:client": "cd client && npm run lint"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.7.3"
  }
}
